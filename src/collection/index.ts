import { Collections as Constructor, ICollections as IConstructor } from './constructor';
import { Strategy, IStrategy } from './strategy';

export {
    IConstructor,
    Constructor,
    Strategy,
    IStrategy,
};
